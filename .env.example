# Elastic Serverless — use the SAME project/deployment for both query and ingest
ELASTIC_ENDPOINT=https://<your-deployment>.es.<region>.cloud.es.io
ELASTIC_API_KEY=<your_api_key>

# OTLP ingest: use the Managed OTLP URL from your project (Add data → OpenTelemetry).
# Must be the same deployment as ELASTIC_ENDPOINT so the app can see the metrics it sends.
OTLP_ENDPOINT=https://<your-deployment>.ingest.<region>.cloud.es.io
OTLP_HEADERS=Authorization=ApiKey <your_api_key>

# Optional: Elastic Agent Builder MCP. When set to your Kibana Agent Builder MCP URL, the Copilot
# uses the Elastic Agent Converse API (same agent as Agent Chat: reasoning + platform.core.search, etc.).
# Example: https://<deployment>.kb.<region>.aws.elastic.cloud/api/agent_builder/mcp
# MCP_SERVER_URL=https://your-deployment.kb.region.aws.elastic.cloud/api/agent_builder/mcp

# Optional: Kibana base URL for Cases API (Ecolab Cases panel). Inferred from MCP_SERVER_URL if unset.
# KIBANA_URL=https://your-deployment.kb.region.aws.elastic.cloud

# Optional: Agent ID for Converse (default: elastic-ai-agent). For Observability set your agent id from Agent Builder.
# ELASTIC_AGENT_ID=elastic-ai-agent

# Optional: deterministic simulator seed
# DEMO_SEED=42

# Optional: interval (ms) for auto-run polling. Server-side default 5000.
# DEMO_INTERVAL_MS=5000
# For client-side polling, set:
# NEXT_PUBLIC_DEMO_INTERVAL_MS=5000

# Copilot: when set, uses OpenAI to summarize Elastic tool results (no Converse, no timeout).
# If unset and MCP_SERVER_URL is Elastic Agent Builder, Converse is used (may timeout).
# OPENAI_API_KEY=sk-...
