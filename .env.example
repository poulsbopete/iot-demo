# Elastic Serverless
ELASTIC_ENDPOINT=https://<your-deployment>.es.us-central1.gcp.cloud.es.io
ELASTIC_API_KEY=<your_api_key>

# OTLP export (often same host as ELASTIC_ENDPOINT; Elastic OTLP intake may use port 443 or 9200)
OTLP_ENDPOINT=https://<your-deployment>.es.us-central1.gcp.cloud.es.io
OTLP_HEADERS=Authorization=ApiKey <your_api_key>

# Optional: external MCP server. If unset, the app uses an internal MCP tool router at /api/mcp.
# MCP_SERVER_URL=https://your-mcp-server.example.com

# Optional: deterministic simulator seed
# DEMO_SEED=42

# Optional: interval (ms) for auto-run polling. Server-side default 5000.
# DEMO_INTERVAL_MS=5000
# For client-side polling, set:
# NEXT_PUBLIC_DEMO_INTERVAL_MS=5000

# Optional: OpenAI for Copilot summarization. If unset, rule-based summarizer is used.
# OPENAI_API_KEY=sk-...
